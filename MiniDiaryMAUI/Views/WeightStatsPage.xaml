<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
    x:Class="MiniDiaryMAUI.Views.WeightStatsPage"
    Title="Statystyki wagi">

    <VerticalStackLayout Padding="20" Spacing="16">

        <!-- Range toggle -->
        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
            <Button
                Text="7 dni"
                Command="{Binding SelectRangeCommand}"
                CommandParameter="7"
                Style="{Binding IsRange7Selected, Converter={StaticResource ActiveTabStyleConverter}}" />

            <Button
                Text="30 dni"
                Command="{Binding SelectRangeCommand}"
                CommandParameter="30"
                Style="{Binding IsRange30Selected, Converter={StaticResource ActiveTabStyleConverter}}" />
        </HorizontalStackLayout>

        <!-- Chart -->
        <lvc:CartesianChart
            HeightRequest="220"
            Series="{Binding Series}"
            XAxes="{Binding XAxes}" />

        <!-- Stats -->
        <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
            <VerticalStackLayout Grid.Column="0" HorizontalOptions="Center">
                <Label Text="Min" FontSize="12" />
                <Label Text="{Binding MinWeightText}" FontAttributes="Bold" />
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                <Label Text="Max" FontSize="12" />
                <Label Text="{Binding MaxWeightText}" FontAttributes="Bold" />
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="2" HorizontalOptions="Center">
                <Label Text="Średnia" FontSize="12" />
                <Label Text="{Binding AverageWeightText}" FontAttributes="Bold" />
            </VerticalStackLayout>
        </Grid>

    </VerticalStackLayout>

</ContentPage>